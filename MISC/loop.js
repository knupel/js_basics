let max = 10;
for (let i = 0; i < max; i++) {
  console.log("j'en suis là", i);
}
let val = 0;
while (val < max) {
  console.log("ma val est", val, "mais ne peut pas être supérieure à", max);
  val++;
}