let ticket = { id: 0};
if(ticket === { id: 0}) {
	console.log("too bad");
} else {
	console.log("is diffenret sorry... {} === {} alway false");
}

if(ticket.id === { id: 0}.id) {
	console.log("but that option is good >>> ticket.id === { id: 0}.id");
}